# Docker Compose image. Pin a release tag or digest for production upgrades.
SILLAGE_IMAGE=ghcr.io/getsillage/sillage:latest

# Keep the service loopback-only unless you intentionally configure a trusted
# LAN listener or an operator-managed HTTPS entry point and host firewall.
SILLAGE_HOST_PORT=127.0.0.1:5231
