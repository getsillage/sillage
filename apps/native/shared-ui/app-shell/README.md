# Native application shell

Reserved for shared Compose navigation structure, top-level layout, feedback
hosts, and feature composition. Platform lifecycle and packaging remain in each
application host.
