# HTTP extensions

Reserved for contract definitions that are not fully represented by the
structured RPC schema, including multipart attachment upload, authenticated
file download, bootstrap headers, and stable REST error envelopes.
