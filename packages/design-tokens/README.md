# Design tokens

Reserved for reviewed, platform-neutral visual tokens such as semantic colors,
spacing, typography roles, motion durations, and accessibility constraints.

This package must not attempt to share rendering components between React and
Compose.
