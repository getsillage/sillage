# Database

Reserved for the shared local schema, migrations, DAO contracts, and
platform-specific SQLite drivers. Workspace data, outbox mutations, cursors,
conflicts, and staged attachments must have explicit transactional boundaries.
