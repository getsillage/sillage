# Synchronization feature

Reserved for user-triggered synchronization progress, durable conflicts,
retryable failures, and sync settings. The synchronization algorithm itself
belongs to `packages/kmp-core/sync/`.
