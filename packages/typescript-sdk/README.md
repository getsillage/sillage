# TypeScript SDK

Reserved for the generated and handwritten transport boundary consumed by the
Web client. Generated DTOs remain internal to the SDK; callers consume stable
client methods and map responses into Web feature models.
