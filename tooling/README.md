# Tooling

`tooling/` is the target location for repository-wide code generation, CI,
release, and policy tooling. Runtime deployment scripts remain under `scripts/`
until the server migration separates runtime assets from development tooling.
